{
  "name": "db",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --check --ignore-path=../../.prettierignore . **/*.svelte && tsc --noEmit && eslint \".\"",
    "format": "prettier --write --ignore-path=../../.prettierignore . **/*.svelte",
    "db:push": "dotenv -e ../../.env -e ../../.env.local -- drizzle-kit generate:sqlite",
    "db:remigrate": "dotenv -e ../../.env -e ../../.env.local -- rm db.sqlite || true && rm -r ./src/migrations || true && pnpm run db:push"
  },
  "devDependencies": {
    "dotenv-cli": "^7.1.0",
    "drizzle-kit": "^0.17.1",
    "eslint-config-custom": "workspace:*"
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.3",
    "better-sqlite3": "^8.2.0",
    "drizzle-orm": "^0.23.2",
    "soundcloud": "workspace:*",
    "spotify": "workspace:*",
    "superjson": "^1.12.2"
  }
}

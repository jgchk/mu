{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "dotenv -e .env -e .env.local -- pnpm --filter server start",
    "docker": "pnpm docker:build && pnpm docker:run",
    "docker:build": "docker build . -t jgchk/mu",
    "docker:run": "dotenv -e .env -e .env.local -- pnpm docker:run:cmd",
    "docker:run:cmd": "docker run --expose $SERVER_PORT -p $SERVER_PORT:$SERVER_PORT --rm jgchk/mu",
    "dev": "test -f ./apps/client/build/index.js && true || turbo run build --filter client && NODE_ENV=development dotenv -e .env -e .env.local -- turbo run dev --concurrency 14",
    "lint": "turbo run lint",
    "format": "prettier --write . **/*.svelte && turbo run format",
    "ci": "pnpm turbo run build --filter client && turbo run ci",
    "db:drop": "rm apps/server/db.sqlite || true && rm -r apps/server/downloads || true && rm -r apps/server/music || true && rm -r apps/server/images || true",
    "db:remigrate": "cd packages/db && pnpm db:remigrate"
  },
  "devDependencies": {
    "eslint": "^8.39.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.8.8",
    "prettier-plugin-svelte": "^2.10.0",
    "prettier-plugin-tailwindcss": "^0.2.7",
    "turbo": "^1.9.3"
  },
  "packageManager": "pnpm@8.0.0",
  "dependencies": {
    "dotenv-cli": "^7.2.1"
  }
}

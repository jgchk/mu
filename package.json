{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "NODE_ENV=development dotenv -e .env -e .env.local -- turbo run dev --concurrency 11",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "ci": "pnpm turbo run build --filter client && turbo run ci",
    "db:drop": "rm apps/server/db.sqlite || true && rm -r apps/server/downloads || true && rm -r apps/server/music || true"
  },
  "devDependencies": {
    "eslint": "^8.36.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.8.7",
    "prettier-plugin-svelte": "^2.10.0",
    "prettier-plugin-tailwindcss": "^0.2.5",
    "turbo": "1.6.3"
  },
  "packageManager": "pnpm@7.15.0",
  "dependencies": {
    "dotenv-cli": "^7.1.0"
  }
}
